<Window x:Class="GuessThePrice.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GuessThePrice"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Content="{Binding MinimumPrice}" Grid.Column="0" Grid.Row="1"/>
        <Label Content="{Binding WinnerText}" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2"/>
        <Label Content="{Binding MaximumPrice}" Grid.Column="1" Grid.Row="1"/>
        <Label Content="{Binding RealPrice}" Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2"/>
        <Label Content="{Binding OpPriceGuess}" Grid.Column="0" Grid.Row="4" HorizontalContentAlignment="Center" VerticalContentAlignment="Bottom"/>
        <Label Content="Oppontent's Guess:" Grid.Column="0" Grid.Row="4" HorizontalContentAlignment="Center" />
        <Button Content="NextRoundCommand" Command="{Binding NextRoundCommand}" Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="2" />
        <StackPanel Grid.Column="1" Grid.Row="4">
            <TextBox Text="{Binding PlayerPriceGuess}" />
            <Button  Content="Make Guess" Command="{Binding PlayerGuessCommand}"  />
        </StackPanel>
    </Grid>
</Window>
